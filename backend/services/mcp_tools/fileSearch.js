module.exports = {
  name: 'file_search',
  description: '在指定目录中搜索文件。',
  handler: async function(params) {
    // This is a placeholder handler for debugging purposes.
    console.log('file_search tool called with params:', params);
    return { success: true, message: 'File search is temporarily disabled for debugging.' };
  }
};